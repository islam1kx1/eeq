<?xml version = "1.0" encoding = "utf-8"?>
<!DOCTYPE hibernate-configuration PUBLIC
        "-//Hibernate/Hibernate Configuration DTD 3.0//EN"
        "http://www.hibernate.org/dtd/hibernate-configuration-3.0.dtd">
<hibernate-configuration>
    <session-factory>
        <!-- Set URL - БД называется salahov -->
        <property name="hibernate.connection.url">
            jdbc:postgresql://localhost:5432/salahov
        </property>

        <!-- Set User Name -->
        <property name="hibernate.connection.username">postgres</property>

        <!-- Set Password -->
        <property name="hibernate.connection.password">root</property>

        <!-- Set Driver Name -->
        <property name="hibernate.connection.driver_class">org.postgresql.Driver</property>

        <property name="hibernate.current_session_context_class">thread</property>

        <property name="hibernate.show_sql">true</property>

        <!-- Optional: Auto-generate schema -->
        <property name="hibernate.hbm2ddl.auto">update</property>

        <!-- ВАЖНО: исправьте путь к вашей модели -->
        <mapping class="ru.zelmex.salahovcourseach.model.ModelLines" />
        <mapping class="ru.zelmex.salahovcourseach.model.Dealers" />
        <mapping class="ru.zelmex.salahovcourseach.model.Shipments" />

    </session-factory>
</hibernate-configuration>